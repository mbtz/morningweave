# MorningWeave USER_TODO

## Development
- Commit the local changes and close tickets: sc-6254, sc-c30d, sc-a58b, sc-d0ee, sc-8edb.
- Publish a Homebrew tap (or add a release tarball + sha256) so `brew install` can use a stable formula without `--HEAD`.
